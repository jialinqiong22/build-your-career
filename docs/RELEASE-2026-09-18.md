# 观己新版开发与部署说明

生产部署：`dpl_782F9dZVujDsLuAcWPoS9ZFrqtQS`，2026-09-18。
正式网址：https://build-your-career-v2.vercel.app
固定快照：https://build-your-career-v2-c8hazoq69-cedars-projects-cede68d1.vercel.app （可能要求Vercel身份验证）。
58项测试、类型检查、lint、本地和Vercel生产构建通过。隐私政策、用户协议和仅邮箱认证已上线；生产接口会拒绝手机号形式的登录，并强制新注册明确同意两份政策。正式网址匿名50题完整作答及PNG生成复测通过；/preview=404，未授权题库=403。Neon账号库、Turnstile与Crisp已启用；完整四维与8页PDF为本机开发流程验收，不代表真实付费流程已联调。

本次依据 `LANDING-PAGE-COPY-V1.md` 与 `UI-AND-ORGANIC-GROWTH-SPEC.md`，价格为免费 / 9.9元 / 99元。旧 `V5-PRODUCT-SPECIFICATION-DRAFT.md` 中79/199元、强制注册免费测评、扩大题库和Kimi方案均未进入本次实现。

## 已实现

- 中性色移动首页、套餐对比、明确标注的演示报告、折叠模块与FAQ。
- 免费50题匿名完成、结果承接、服务端PNG主动生成与下载。
- 标准版50题大五＋40题价值观＋18题兴趣＋五类经历，99元版独立120题大五、30项细分、PDF及人工沟通权益；36题原创九型仍可跳过。
- 登录模态框仅提供邮箱与密码认证；注册必须同意隐私政策与用户协议。服务端拒绝手机号形式的注册和登录，微信与短信入口均已移除。每次敏感动作重新核验会话，成功后继续原操作。
- 用户主动同意后保存云端；每人每种测评一份最新快照。账号隔离、有限请求体、UUID版本冲突检查、主动恢复和删除。50题与120题本机记录分开。
- HMAC签名套餐码绑定用户ID；银行题库、报告、PDF入口逐次检查账号与套餐。标准版不提供产品内PDF流程，浏览器自行打印可见内容无法禁止。
- Chart.js兴趣雷达图、已有大五条形图、打印样式。报告仍为规则生成，不调用LLM。
- 双方明确同意的邀请对比；单条经历反馈、提交前预览、撤回、反馈删除凭据。令牌使用32字节随机数，数据库仅存哈希；所有结果接口禁止缓存。
- 邀请页禁止referrer；Crisp仅加载于普通页面，在 `/compare/` 与 `/feedback/` 私人邀请URL中禁用。

## 上线配置状态

2026-09-18已将 Neon 项目 `观己 build your career` 的 `production` 分支接入 Vercel Production。账号 Drizzle 迁移、`assessment_progress` 与 `social_invites` 表均已执行并核验；运行时使用 pooled URL，迁移使用 direct URL。`AUTH_ENABLED=true`，生产会话密钥独立生成，未复用开发密钥。

Turnstile生产Site Key与Secret已配置，注册页已验证加载Cloudflare iframe；Crisp Website ID已配置并在普通页面验证加载。固定正式域名仍为Vercel域名，购买自定义域名后需同步更新 `NEXT_PUBLIC_APP_URL` 与Turnstile允许的hostname。微信与短信仍需另行提供供应商/平台资质并完成真实认证实现。

邀请需执行 `scripts/social-migration.sql`，确认告知/保留/删除规则后设 `SOCIAL_ENABLED=true`。对比7天、反馈30天到期后API立即拒绝返回；实际物理删除需定期运行 `scripts/cleanup-social.mjs`，当前未配置定时运行。未启用前不收集这些数据。

启用付费需 `PREMIUM_ACCESS_SECRET` 与真实 `PREMIUM_CONTACT`，确认到账后向指定用户发码；无自动支付、自动退款或预约系统。详见 `PAID-OPERATIONS.md`。

## 验证及限制

通过单元测试、类型检查、lint及生产构建后部署。浏览器验证包含匿名50题完整作答、120题完整四维本机预览、可选九型跳过、报告、PNG、窄屏与PDF。Production运行状态返回账号已配置，Crisp普通页加载且邀请页不加载，Turnstile iframe加载；仍需由真人完成一次Turnstile后执行正式注册、登录、退出与云端保存验收。

本地 `/preview` 仅用于开发验收，生产必须404。原有测试数据与正式用户数据未做自动转换。后续正式启用前，还需真实账号兑换、双账号隔离/冲突、双方邀请撤回和定时清理集成验收。
